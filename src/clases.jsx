import React from "react";
import ReactDOM from "react-dom/client";
import Clases from "./components/clases.jsx"; // 👈 Importamos el componente


ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Clases /> 
    
  </React.StrictMode>
);