import React from "react";
import ReactDOM from "react-dom/client";
import Tablon from "./components/tablon.jsx"; // 👈 Importamos el componente


ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Tablon /> 
    
  </React.StrictMode>
);