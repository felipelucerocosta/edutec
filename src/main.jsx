import React from "react";
import ReactDOM from "react-dom/client";
import Registro from "./components/Registro.jsx"; // 👈 Importamos el componente


ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Registro /> 
    
  </React.StrictMode>
);